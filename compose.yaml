services:
  postgre:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: 'root'
      POSTGRES_DB: 'colabora-pro'
    volumes:
      - ./postgres-local:/docker-entrypoint-initdb.d
